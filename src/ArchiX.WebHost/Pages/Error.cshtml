@page
@model ArchiX.WebHost.Pages.ErrorModel
@{
    ViewData["Title"] = "Hata Oluþtu";
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Hata</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
</head>
<body>
    <div class="container mt-5">
        <div class="alert alert-danger shadow">
            <h1><i class="fas fa-exclamation-triangle"></i> Bir Hata Oluþtu</h1>
            <p class="lead">Ýþlem sýrasýnda beklenmeyen bir hata meydana geldi.</p>
            
            @if (Model.ShowRequestId)
            {
                <hr />
                <p><strong>Request ID:</strong> <code>@Model.RequestId</code></p>
            }
            
            <hr />
            <a href="/" class="btn btn-primary"><i class="fas fa-home me-2"></i>Ana Sayfaya Dön</a>
        </div>
    </div>
</body>
</html>
